---
title: "Beyond Global Editing: Per-Instance Disentangled Subspaces for Training-Free Hallucination Mitigation in LVLMs"
title_zh: 超越全局编辑：面向大型视觉语言模型免训练幻觉缓解的逐实例解耦子空间
authors: "Ali Cheraghian, Hamidreza Dastmalchi, Hamed Barzamini, Morteza Saberi, Mojtaba Golzan, Shafin Rahman, Hossein Rahmani"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09344v1"
tags: ["query:cp"]
score: 9.0
evidence: 模型编辑离线修改内部表示
tldr: 大视觉语言模型常因视觉幻觉导致描述失实，影响可靠性。现有免训练模型编辑方法多基于全局子空间，无法适应各输入不同的幻觉模式。我们提出一种动态按实例解耦的幻觉子空间框架，自适应计算权重，以选择性抑制最可能幻觉方向，同时保留图像语义。在多个视觉语言基准及LVLM系列上取得一致性能提升，验证了方法鲁棒、泛化性强且高效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.09344v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1332, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09344v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1327, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09344v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1332, \"height\": 384, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.09344v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1336, \"height\": 431, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09344v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1331, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09344v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 976, \"height\": 247, \"label\": \"Table\"}]"
motivation: 现有免训练模型编辑方法使用单一全局子空间，无法有效应对不同输入样本多样化的幻觉模式，导致缓解效果有限。
method: 构建一组解耦幻觉子空间，分别隔离不同幻觉模式；推理时自适应计算输入与子空间关联权重，动态组合投影以选择性抑制幻觉方向并保留语义。
result: 在多个视觉语言基准和LVLM系列上的实验表明，方法取得一致性能提升，幻觉显著减少。
conclusion: 所提出免训练动态按实例解耦子空间方法有效缓解LVLM幻觉，展现鲁棒性、泛化性和高效性，为可靠多模态推理提供新途径。
---

## 摘要
近期大规模视觉语言模型（LVLMs）通过将视觉编码器与大型语言模型（LLMs）相集成，实现了强大的多模态推理能力。然而，它们的可靠性经常受到幻觉的破坏，即生成的文本不准确地描述了视觉输入。尽管微调可以缓解这一问题，但其计算成本高昂且需要大型精心策划的数据集，这使得免训练的替代方案具有吸引力。在这些替代方案中，模型编辑比基于解码的方法更具前景：解码方法针对每个输入调整输出，但会带来计算开销和不稳定性，而模型编辑则离线修改内部表示，提供了更高效和稳定的解决方案。然而，现有模型编辑技术通常依赖于单一的全局子空间来纠正幻觉，将所有测试样本同等对待，未能捕捉到跨输入的不同幻觉模式。为了解决这一局限性，我们提出了一种免训练的幻觉缓解框架，用于在测试时进行动态的、逐实例的抑制。我们的方法首先构建一组解耦的幻觉子空间，每个子空间隔离一种不同的幻觉模式。在推理过程中，模型自适应地计算反映每个输入与这些子空间关系的权重，从而引导一种动态组合投影，该投影选择性地抑制最可能的幻觉方向，同时保留基于图像的语义信息。在多个视觉语言基准和LVLM家族上的广泛实验展示了一致的改进，突显了我们方法的鲁棒性、泛化性和效率。

## Abstract
Recent advances in large vision-language models (LVLMs) have enabled powerful multimodal reasoning by integrating visual encoders with large language models (LLMs). However, their reliability is frequently undermined by hallucinations, where generated text inaccurately describes the visual input. Although fine-tuning can mitigate this problem, it is computationally expensive and requires large, curated datasets, making training-free alternatives attractive. Among these, model editing is more promising than decoding-based approaches: decoding methods adapt outputs per input but introduce computational overhead and instability, whereas model editing modifies internal representations offline, providing a more efficient and stable solution. However, existing model-editing techniques typically rely on a single global subspace to correct hallucinations, treating all test samples identically and failing to capture diverse hallucination modes across inputs. To address this limitation, we propose a training-free hallucination mitigation framework for dynamic, per-instance suppression at test time. Our method first constructs a set of Disentangled Hallucination Subspaces, each isolating a distinct hallucination mode. During inference, the model adaptively calculates weights reflecting each input's relationship to these subspaces, guiding a dynamically combined projection that selectively suppresses the most probable hallucination directions while preserving image-grounded semantics. Extensive experiments across multiple vision-language benchmarks and LVLM families demonstrate consistent improvements, highlighting the robustness, generalizability, and efficiency of our approach.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：大型视觉语言模型（LVLMs）在生成文本时经常出现“幻觉”——生成的描述与输入图像不符，严重影响模型的可靠性和实用性。
- **研究背景**：已有的幻觉缓解方法主要分为微调与免训练两类。微调代价高昂、数据依赖性强；免训练方法中的模型编辑（如Nullu）因离线修改内部表示而比解码方法更稳定高效。
- **关键局限**：现有模型编辑方法依赖**单一全局子空间**来修正幻觉，对所有测试样本同等处理，无法捕捉不同输入下多样化的幻觉模式。
- **整体含义**：论文提出一种训练无关、测试时动态逐实例抑制幻觉的框架，通过解耦多种幻觉模式并自适应组合校正，提升LVLMs的事实一致性与泛化能力。

### 2. 方法论

核心思想围绕“解耦的多维幻觉子空间 + 测试时实例特定的自适应投影”。

#### 2.1 离线阶段：构建解耦幻觉子空间
- **对比数据集构建**：利用图像、真实描述和由强语言模型（如GPT‑4）生成的语义不一致（幻觉）描述，形成三元组数据集 `D`。
- **激活差异提取**：对每一样本 `i`，分别输入真实/幻觉描述，提取每层 `ℓ` 的token平均隐藏状态 `z(i)_ℓ` 与 `~z(i)_ℓ`，计算差异 `D_ℓ = Z̃ℓ − Zℓ`，再对多层求平均得到 `D̄`，每行为一个样本的语义漂移向量。
- **聚类与子空间构建**：对 `D̄` 的行进行**K‑means聚类**，将样本划分到 `K` 个簇，每个簇代表一种不同的幻觉模式。再对每个簇内差异矩阵进行**奇异值分解（SVD）**，取前 `r` 个右奇异向量构成正交基 `V^{(k)}_r ∈ R^{d×r}`，得到低秩幻觉子空间集合 `V = {V^{(k)}_r}_{k=1}^K`。
- **性质**：每个子空间是正交投影子，`I − V^{(k)}_r V^{(k)⊤}_r` 是其正交补空间投影，保证操作数值稳定。

#### 2.2 测试时阶段：自适应投影抑制
- **扰动探查**：对测试图像 `I`，生成二值掩码 `M`（掩盖 70% 语义显著区域），得到 `Ĩ = I ⊙ M`。将原始图和掩码图分别输入模型，提取选定层隐藏状态，计算差异向量 `δ_{ℓ,j}`（掩盖‑原始）。
- **子空间对齐评分**：将 `δ_{ℓ,j}` 投影到各个子空间 `V^{(k)}_r`，投影范数 `s_{k,ℓ} = ‖δ⊤ V^{(k)}_r‖²` 作为该输入与第 `k` 种幻觉模式的关联分数。
- **权重计算**：对每层 `ℓ`，利用 `softmax` 归一化 `s_{k,ℓ}` 得到 `α_{k,ℓ}`；再跨层聚合为 `γ_k = ∑_ℓ α_{k,ℓ}`，并施加温度 `τ` 的 `softmax` 得到全局自适应权重 `β_k`。
- **组合投影与抑制**：构建样本特定的投影矩阵 `P = ∑_k β_k V^{(k)}_r V^{(k)⊤}_r`。在模型前向过程中，将选定层的隐藏状态替换为 `z^(edited) = (I − P)z`，从而**选择性移去在样本所混和幻觉子空间内的成分**，保留语义内容。
- **性质**：`P` 对称半正定，`‖P‖_2 ≤ 1`，`I−P` 为广义收缩，不放大激活分量，保护生成流畅性。整个干预仅额外增加一次掩码前向传播，计算开销极小。

### 3. 实验设计

- **基准数据集**：
  - **CHAIR**：评估图像描述中的对象幻觉，指标为 CHAIR S（句子级）、CHAIR I（实例级）和 BLEU（流畅度）。
  - **POPE**：通过是否包含特定物体的问答评估幻觉，包含“随机”、“频繁”、“对抗”三种设置；还采用了离线 POPE（OPOPE）变体，判断生成描述中是否有不应存在的物体。
- **测试模型**：LLaVA‑1.5、MiniGPT‑4、mPLUG‑Owl2，覆盖线性投影、Q‑Former、模态协作等不同架构。
- **对比方法**：Greedy、Beam Search、DoLa、OPERA、VCD、Woodpecker、LURE、HALC、Nullu，涵盖解码策略、投影抑制、后校验等不同类型。
- **主要实验设置**：每个模型和基准上针对性地设置子空间簇数 `K` 和基向量数 `r`（如 LLaVA‑1.5 在 CHAIR 上用 7 簇 4 基，mPLUG‑Owl2 用 5 簇 32 基等）。均采用 MSCOCO 验证集，10 次独立运行报告均值和标准差。

### 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量及任何训练或推理阶段的硬件细节。由于方法完全免训练，仅须在测试时进行少量前向推理（额外一次掩码前向传播），对算力需求极低，实际开销几乎可以忽略。

### 5. 实验数量与充分性

- **对比实验数量**：在 3 种 LVLM × 2 个基准（CHAIR 和 POPE）上，与至少 9 种基线方法进行全面对比，总计约 54 组主结果。
- **消融实验**：
  - 子空间个数 `K` 的影响（LLaVA‑1.5、MiniGPT‑4、mPLUG‑Owl2）
  - 不同图像扰动策略（掩码、高斯噪声、模糊）的效果
  - 基向量数 `r` 的影响（1 ~ 64）
- **实验充分性与公平性**：
  - 涵盖不同模型架构和多样化评估任务，对比基线丰富，统计上给出标准差以体现稳定性。
  - 超参数（`K`, `r`）均在训练集上优化再用于测试，比较公平。
  - 但消融实验部分仅基于 LLaVA‑1.5 模型，其他模型的消融展示较少。

### 6. 主要结论与发现

- 所提方法的 CHAIR 指标在所有三个模型上均优于所有基线（包括 Nullu），同时在 BLEU 上保持或略微提升，表明其**有效减少幻觉且不牺牲生成质量**。
- 在 POPE 上，对 mPLUG‑Owl2 的提升尤为明显（F‑score 从 90.80 提至 91.68），对 LLaVA‑1.5 和 MiniGPT‑4 则持平或略优，说明该方法对**更强视觉融合结构**的模型增益更大。
- 消融表明：子空间个数和基向量维度存在最优平衡，过多过少均会降低效果；**掩码扰动**相比高斯噪声和模糊更能产生有效的幻觉方向探测信号。
- 方法完全免训练，基于预计算的子空间和轻量的掩码前传，计算开销小，易于部署。

### 7. 优点

- **创新性强**：首次提出解耦多个低秩子空间，并实现测试时逐实例自适应组合，克服了全局单一子空间的局限性。
- **实用且高效**：免训练、即插即用，仅需一次额外前向传播，不修改模型参数，保持模型原始能力与生成流畅性。
- **性能提升稳健**：在多个 LVLM 和基准上一致优于现有免训练方法，尤其对强模型效果突出。
- **理论支撑扎实**：从聚类到 SVD 构建正交基，再到投影的广义收缩性质，保证了干预的数值稳定性和语义保留。
- **可解释性**：通过子空间可视化或分析，可理解不同幻觉类型的表示，利于未来调试。

### 8. 不足与局限

- **离线数据集依赖**：需要构建高质量的成对（真实/幻觉）对比语料来生成子空间，且依赖外部强语言模型生成幻觉描述，可能引入生成偏差。
- **部分模型增益有限**：对 MiniGPT‑4 等在 POPE 上提升不显著，可能与某些模型视觉压缩程度过高导致子空间信号弱有关。
- **超参数敏感性**：簇数 `K`、基维度 `r`、温度 `τ`、扰动程度（掩码比例、选取区域）等需在特定模型和任务下调整，通用性受制。
- **幻觉类型覆盖**：虽然解耦了多个方向，但可能仍无法涵盖所有细粒度的幻觉（如关系、属性级别），且受限于训练对比数据中的模式分布。
- **推理速度**：虽整体开销小，但每次推理均需额外进行一次掩码图像的前向传播，对实时性要求极高的场景可能仍会产生轻微影响。
- **公平性考量**：文中未报告所使用的掩码生成具体算法，不同掩码区域选择策略可能对结果有未知影响。

（完）
