---
title: "ConFusion: Continuous Fusion Space Learning for Fine-Grained Controllable Infrared and Visible Image Fusion"
title_zh: "ConFusion: 面向细粒度可控红外与可见光图像融合的连续融合空间学习"
authors: "Guo Yurong, He Yufei, Li Yonghao, Chang Dongliang, Zhang Ke, Ma Zhanyu"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23600v1"
tags: ["query:cp"]
score: 9.0
evidence: 双分支架构解耦模态不变（形状）和模态特有（颜色/纹理）特征
tldr: 现有的可控红外可见图像融合方法通常依赖预定义的离散控制条件，导致融合控制空间稀疏，难以满足细粒度调整需求。为此，本文提出ConFusion框架，通过高斯条件空间感知调制学习连续融合空间，实现实例级别的精细控制。实验表明，该方法在融合质量和下游任务中均达到最优性能，并支持灵活的用户意图驱动融合。该工作为细粒度可控多模态融合提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23600v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 792, \"height\": 906, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23600v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1654, \"height\": 942, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23600v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 933, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23600v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1657, \"height\": 714, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23600v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1662, \"height\": 723, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23600v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 806, \"height\": 858, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23600v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 810, \"height\": 317, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23600v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1342, \"height\": 378, \"label\": \"Table\"}]"
motivation: 现有方法依赖离散控制条件，融合控制空间稀疏，难以实现细粒度调制。
method: 提出ConFusion框架，通过高斯条件空间感知调制和实例掩码引导，学习连续融合空间，实现实例级可控融合。
result: 在多个融合质量与下游任务指标上达到最优，并支持用户意图驱动的细粒度可控融合。
conclusion: ConFusion通过学习连续融合空间，突破了离散控制的限制，为细粒度的多模态图像融合提供了新思路。
---

## 摘要
可控红外-可见光图像融合旨在通过灵活的区域感知调制整合互补的热信息和结构信息，生成适应多样化用户需求和下游任务的融合图像。然而，现有方法通常依赖预定义的离散控制条件，导致稀疏的空间无法支持细粒度调制需求。为此，我们提出ConFusion，一个通过高斯条件化空间感知调制学习连续融合空间的新框架，实现实例级细粒度可控红外与可见光图像融合。ConFusion采用双分支架构，在联合重建和文本引导语义对齐下解耦模态不变和模态特定表示。高斯条件化的实例调制变量与基于Grounded SAM的实例掩码一起，通过掩码引导的特定特征调制器指导实例级细粒度调制，而文本驱动的不变特征增强器改善语义一致性并提升融合效果。在推理时，多模态大语言模型将用户意图解析为实例级调制变量以指导图像融合。大量实验表明，ConFusion在融合质量和下游任务的多个指标上实现了最先进的性能，同时支持细粒度可控图像融合。我们的代码可在https://github.com/HeyufeiAnto/Confusion获取。

## Abstract
Controllable infrared-visible image fusion aims to integrate complementary thermal and structural information with flexible region-aware modulation, producing fused images that adapt to diverse user requirements and downstream tasks. However, existing methods typically rely on predefined discrete control conditions, leading to a sparse space that fails to support fine-grained modulation demands. To address this, we propose ConFusion, a novel framework that learns the continuous fusion space via Gaussian-conditioned spatial-aware modulation, enabling instance-level fine-grained controllable infrared and visible image fusion. ConFusion employs a dual-branch architecture to disentangle modality-invariant and modality-specific representations under joint reconstruction and text-guided semantic alignment. Gaussian-conditioned instance modulation variables coupled with Grounded SAM-based instance masks guide instance-level fine-grained modulation through the Mask-Guided Specific Feature Modulator, while the Text-Driven Invariant Feature Enhancer improves semantic consistency and enhances fusion. During inference, the multimodal large language model parses user intents into instance-level modulation variables to guide image fusion. Extensive experiments show that ConFusion achieves state-of-the-art performance across multiple metrics in both fusion quality and downstream tasks, while supporting fine-grained controllable image fusion. Our code is available at https://github.com/HeyufeiAnto/Confusion

---

## 论文详细总结（自动生成）

## ConFusion 论文结构化总结与分析

### 1. 论文的核心问题与整体含义
*   **研究背景与动机**：红外-可见光图像融合（IVIF）旨在整合热辐射信息与纹理细节。传统非可控融合方法输出单一，无法满足不同用户或任务的偏好。近年来兴起的可控融合方法（如文本驱动）虽能实现实例级控制，但受限于**预定义的离散控制条件**，只能生成稀疏的融合解空间。
*   **核心问题**：现有粗粒度可控融合面对语义相近但程度不同的指令（如“高热”与“极热”）时，往往坍缩为几乎相同的响应（相关系数相同），**缺乏细粒度的连续调制能力**。
*   **整体含义**：本文提出学习一个**连续的融合空间**，使得模型能从高斯分布中密集采样控制变量，从而对每个实例进行精确到细微差别的独立调控。

### 2. 方法论
*   **核心思想**：**高斯条件化空间感知调制（Gaussian-conditioned spatial-aware modulation）**。通过从连续分布中采样实例级变量来控制融合强度，替代传统的离散条件。
*   **关键架构与技术细节**：
    *   **第一阶段：解耦表征学习**
        *   **特征解耦**：共享底层编码器后接双分支结构，分别提取模态不变（场景语义）与模态特定特征（热辐射/纹理）。利用自重建和跨重建损失进行约束。
        *   **解耦优化**：引入解耦损失 \( \mathcal{L}_{dis} \)（最小化互协变量的 Frobenius 范数）和不变损失 \( \mathcal{L}_{inv} \)（MSE）。
        *   **语义对齐**：使用 CLIP 文本嵌入作为语义锚点，通过 Grounded SAM 分割掩码对不变特征进行语义池化，并利用余弦相似度损失 \( \mathcal{L}_{text} \) 对齐特征与文本。
    *   **第二阶段：特征调制与增强融合**
        *   **掩码引导的特定特征调制器（MGSM）**：对输入图像中的每个实例 \( o_j \) 从连续高斯分布（\( \mathcal{N}_{[0,1]}(0.5, 0.2^2) \)）采样调制强度 \( \alpha_j \)。结合 Grounded SAM 生成的掩码构建**空间感知调制掩码** \( M \)。通过卷积学习仿射变换参数，对红外分支使用掩码 \( M \) 调制，对可见光分支使用互补掩码 \( (1-M) \) 调制。利用 Restormer 模块融合调制后的特定特征，并施加感兴趣区域（ROI）约束损失 \( \mathcal{L}_{ROI} \)。
        *   **文本驱动的不变特征增强器（TDIE）**：利用交叉注意力机制将 CLIP 文本嵌入 \( E \) 注入融合后的不变特征 \( F^{in}_{fu} \)，生成语义增强的不变特征，并施加背景重建损失 \( \mathcal{L}_{bg} \)。
    *   **推理流程**：多模态大语言模型（LLM）解析用户意图与源图，生成实例-强度对和实例文本；结合 Grounded SAM 和 CLIP 编码器驱动物理和语义调制，生成最终融合图。

### 3. 实验设计
*   **数据集与场景**：
    *   训练与综合测试：`M3FD`（3900对训练，300对测试）。
    *   泛化性测试：`RoadScene`（220对）、`TNO`（37对）。
*   **Benchmark 与对比方法**：与7种最先进的 SOTA 方法进行对比，涵盖非可控方法（RFNNest, TarDAL, CDDFuse, CoCoNet, TDFusion）及可控方法（TextFusion, RISFuse）。
*   **评估指标**：融合质量指标包括 PSNR, MSE, AG, SF, CC, Qabf；下游任务指标为 mAP50 等（基于 YOLOv9 检测器评估）。

### 4. 资源与算力
*   **硬件配置**：单卡 **NVIDIA RTX 4090** GPU，搭配 AMD EPYC 7453 28核 CPU。
*   **软件环境**：PyTorch 2.9.1。
*   **训练策略**：输入裁剪为 224×224，批次大小为 2。
    *   第一阶段：50 epoch，学习率 1e-4。
    *   第二阶段：50 epoch，主干学习率 1e-6，新增模块 1e-4。

### 5. 实验数量与充分性
*   **实验体量**：实验设计极其全面，涵盖了**多数据集对比、消融研究、可控性分析及下游应用验证**。
*   **充分性与公平性**：
    *   **对比全面**：在3个公开基准上与7种近年主流方法（含2021-2025年论文）横评。
    *   **消融完整**：在 M3FD 上针对语义对齐、MGSM 模块、TDIE 模块、\( \mathcal{L}_{cr} \)、\( \mathcal{L}_{dis} \)、\( \mathcal{L}_{inv} \) 进行了6组消融实验，证实了各组件与损失函数的必要性。
    *   **定性定量兼备**：既提供了融合图像的可视化对比，也给出了像素级指标表格；在细粒度控制实验中，通过可视化结果和**相关系数（CC）**的渐变折线图，直观论证了连续控制的有效性。

### 6. 主要结论与发现
*   提出的 ConFusion 在 M3FD、RoadScene 和 TNO 数据集上取得了领先的定量指标，表明其具有优异的保真度和细节保留能力。
*   通过对比“非可控”、“粗粒度可控”与本文方法，证明了连续空间学习能准确响应“低-中-高-极高”等细微意图差异，实现了真正意义上的**实例级细粒度可控融合**。
*   在下游目标检测任务中，ConFusion 生成的融合图同样达到了最高精度的综合得分，验证了该方法对机器视觉任务的增益。

### 7. 优点与亮点
*   **范式创新**：首次将 IVIF 的可控性问题建模为从离散空间向**连续融合空间**的迁移，允许无限且细粒度的调制。
*   **机制精致**：将高斯条件变量与物理实例掩码耦合，结合双分支解耦架构，做到了语义特征（不变）与物理属性（特定）的精准独立调控。
*   **评估严谨**：控制变量实验（图5）极具说服力，不仅对比了有无控制，更量化了“控制程度”的线性变化，这在过往融合论文中较少见。
*   **全栈实现**：打通了从 LLM 自然语言理解到感知模型实例分割，再到双分支网络物理融合的自动化全流程。

### 8. 不足与局限
*   **计算开销**：论文坦承 LLM 和 Grounded SAM 的引入显著增加了推理算力负担，难以直接部署在极端边缘设备。
*   **预处理依赖**：性能高度依赖 Grounded SAM 的分割精度；若出现漏检或误检实例，调制掩码将失效，且模型对此缺乏容错机制。
*   **泛化风险**：TNO 数据集体量极小（37对），公路场景（RoadScene）相对单一，模型在面对更复杂的全天候或剧烈噪声环境时的鲁棒性尚未验证。
*   **意图歧义**：LLM 对模糊的自然语言意图（如“稍微明显一点”）的解析可能有所偏差，缺乏将度量属性转化为高斯空间映射的校准标准。

（完）
