---
title: LoRA for Gender-Inclusive Rewriting and Activation Steering for Counter-Narrative Generation
title_zh: 面向性别包容性重写的 LoRA 与用于反叙述生成的激活引导
authors: "Akhil Rajeev P, Manoj Balaji J"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23083v1"
tags: ["query:cp"]
score: 9.0
evidence: 通过PCA在隐藏状态上激活引导
tldr: "性别包容生成任务需将偏见文本转化为公正表达，反叙事生成尤需轻量可控方案。本文提出IHLC，对重写采用LoRA微调，对反叙事利用PCA从对比激活提取方向，注入Gemma-3-4B-it中间层实现激活转向，辅以约束提示。在共享任务中，重写与反叙事分别获80.00%和78.12%评分，分析发现语义漂移等失效模式。工作证实激活转向作为无权重更新方法的可行性，同时指出层敏感性等局限。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23083v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 443, \"label\": \"Table\"}]"
motivation: 针对反叙事生成，探索无需修改模型权重的计算高效推理时控制方法。
method: 对重写用LoRA微调；对反叙事用PCA从对比隐藏状态导出方向，注入Gemma-3-4B-it中间层实现激活转向，结合约束提示。
result: "重写和反叙事子任务分别取得80.00%和78.12%官方评分；人工分析识别语义漂移、偏见泄露等失效模式。"
conclusion: 激活转向无需权重更新，可作为轻量可控生成方案，但存在层敏感性、过度转向等局限。
---

## 摘要
性别包容性语言生成旨在将带有偏见的文本转换为包容性替代表达，同时保留语义含义和上下文连贯性。本文介绍了针对 LT-EDI 2026 共享任务提出的 IHLC 系统，该系统同时处理性别包容性重写和反叙述生成。对于性别包容性重写，我们采用参数高效的低秩适应（LoRA）微调，获得了 80.00% 的官方分数。我们的主要贡献是一种用于反叙述生成的计算高效、推理时表示工程方法。我们利用主成分分析（PCA）从对比隐藏状态激活中提取主引导方向，并在推理过程中将其注入 Gemma-3-4B-it 的中间表示，从而在不修改模型权重的情况下实现向包容性回应的行为引导。结合约束提示，该方法能产生礼貌且上下文恰当的反叙述，取得了 78.12% 的官方分数。我们进一步对引导行为进行了人工分析，识别出关键失效模式，包括语义漂移、残余偏见泄露、层敏感性、过度引导和文本退化。我们的发现突显了激活引导作为一种轻量级参数更新替代方案，在可控且符合社会价值的语言生成中的实际潜力和当前局限性。

## Abstract
Gender-inclusive language generation seeks to transform biased text into inclusive alternatives while preserving semantic meaning and contextual coherence. This paper presents the IHLC system for the LT-EDI 2026 Shared Task, addressing both gender-inclusive rewriting and counter-narrative generation. For gender-inclusive rewriting, we employ parameter-efficient Low-Rank Adaptation (LoRA) fine-tuning, achieving an official score of 80.00%. Our primary contribution is a compute-efficient inference-time representation engineering approach for counter-narrative generation. We derive a principal steering direction from contrastive hidden-state activations using principal component analysis (PCA) and inject it into the intermediate representations of Gemma-3-4B-it during inference, enabling behavioral steering toward inclusive responses without modifying model weights. Combined with constrained prompting, this approach produces polite and contextually appropriate counter-narratives, achieving an official score of 78.12%. We further present a manual analysis of steering behavior, identifying key failure modes including semantic drift, residual bias leakage, layer sensitivity, over-steering, and text degeneration. Our findings highlight both the practical potential and current limitations of activation steering as a lightweight alternative to parameter updates for controllable and socially aligned language generation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

本文聚焦于 **LT-EDI 2026 共享任务** 的两项核心挑战：性别包容性重写（Subtask A）与反叙述生成（Subtask B）。整体目标是自动将带有性别偏见或刻板印象的文本转化为性别中立、包容的表达，同时保持语义完整性。

- **研究动机与背景**：  
  传统微调方法（如全参数更新）计算成本高，难以在资源有限或需要快速部署时使用。此外，反叙述生成不仅需要消除偏见，还需输出礼貌、连贯且能有效对抗偏见话语的文本。  
  论文旨在探索 **计算高效的推理时干预方法**，特别是 **激活引导（activation steering）** 技术，以在不修改模型原始参数的情况下，将预训练语言模型的行为导向包容性输出。

### 2. 论文提出的方法论

论文针对两个子任务采用不同策略，其中反叙述生成的方法论为核心创新：

#### **Subtask A：LoRA 微调（参数高效微调）**
- 采用 **低秩适应（LoRA）** 对模型进行轻量级适配，通过在预训练权重上注入低秩矩阵，仅更新少量参数。
- 训练数据为共享任务提供的英文句子改写对（如 *fireman → firefighter*）。

#### **Subtask B：激活引导 + 约束提示（无权重更新）**
核心思路是从 **对比隐藏状态激活** 中提取一个“包容性方向”，在推理时注入指定层，引导模型生成更中立的文本。

**关键技术细节：**
1. **对比激活对构建**  
   - 收集成对的有偏/中立句子作为正负样本（counterfactual pairs）。  
   - 使用 Gemma-3-4B-it 提取这些句子在 **指定 Transformer 层**（最终选定第16层）的 **最后一个 token 的隐藏状态**，计算每对的差异向量。
2. **引导方向抽取**  
   - 对所有差异向量应用 **主成分分析（PCA）**，取第一主成分作为 **引导向量（steering vector）**。该方向代表了从偏见到中立的语义转变方向。
3. **推理时注入**  
   - 在推理时注册一个 **前向钩子（forward hook）**，对于每一 token 位置（或仅最后一个 token），将该层的隐藏状态加上 **α × 引导向量**（α 为引导强度系数，推荐范围 0.6–1.2）。  
   - 配合 **严格提示模板**（如要求输出单行、仅包含最终句子）来抑制非必要解释，提升自动评估得分。
4. **解码策略**  
   - 使用低温度采样与重复惩罚，以防止 α 过大时出现文本循环退化。

### 3. 实验设计

- **数据集与基准**：  
  使用共享任务官方提供的英文数据集（Subtask A 的改写对与 Subtask B 的反叙述对）。  
  评估采用组织者设计的 **混合评估框架**：LLM-as-judge（基于固定量规）辅以人工抽查与裁决。
  - Subtask A 指标：性别假设移除度（GA）、性别中立性（GN）、质量与相关性（QR），取三者平均为总分。
  - Subtask B 指标：礼貌与尊重度（PR）、上下文反叙述连贯性（CCNC）、质量与相关性（QR），平均为总分。

- **对比方法/实验变体**：  
  论文未提供与传统微调的大量系统对比，而是通过 **控制变量实验** 在自身方法内进行分析，包括：
  - 引导强度 α 的取值变化；
  - 不同提示模板（软性 DEI 提示 vs. 严格单行提示）；
  - 解码策略（贪婪解码、采样、重复惩罚）的影响。

  另外，论文内部对生成结果进行了 **手动分析**，基于 47 个测试样例识别失败模式。

### 4. 资源与算力

- 文中 **未明确说明** 训练所使用的 GPU 型号、数量或具体时长。仅提及模型 Gemma-3-4B-it 以及实现基于 PyTorch/HuggingFace。所有代码被导出为单一 Jupyter notebook。任务本身强调计算高效性，但未给出使用的硬件配置或算力消耗的具体数值。

### 5. 实验数量与充分性

- 论文提交了两项子任务的系统结果，各包含全套自动评估指标，并在 Subtask B 上进行了一次系统的 **手动错误分析**（47 个样本，分为五种失败模式并统计分布）。  
- 实验的充分性表现如下：
  - **充分方面**：手动分析揭示了激活引导的典型失效模式，具有诊断价值；对不同超参数（α、提示模板、解码策略）的影响有实践性总结。
  - **局限方面**：缺乏与微调 baseline 或其他表示工程方法的定量对比实验；只在单种模型上验证；没有跨语言或更多数据集的泛化结果；未进行统计显著性检验。因此，结论的推广性仍有保留。

### 6. 论文的主要结论与发现

- **总体表现**：  
  - Subtask A（LoRA 微调）取得 80.00% 平均分，排名第 3（共39队）。  
  - Subtask B（激活引导）平均 78.12%，其中礼貌度和上下文连贯性（~85%）较高，但相关性得分较低（~65%），导致排名第 6（共67队）。
- **超参数敏感性**：  
  引导强度 α 过低则中立性不足，过高则引发重复循环与幻觉；严格提示模板有助于提升自动评分。
- **失效模式分析**（基于 47 个样本）：  
  1. **忠实度-编辑权衡**（53%）：输出偏离原意，过度泛化。  
  2. **层特定纠缠敏感**（23%）：单一层的单向引导难以解耦相关概念。  
  3. **残余偏见泄露**（11%）：偏见仅被表层掩盖，负面前提仍被保留。  
  4. **过度引导不稳定**（9%）：α 过大导致句子结构崩溃或语义偏离。  
  5. **文本退化与重复**（4%）：出现 token 粘连或循环。
- 证明激活引导可在无权重更新的情况下控制生成方向，但对于复杂的社会语义操纵仍不够精细和稳定。

### 7. 优点

- **方法简洁且计算成本低**：利用 PCA 方向引导，无需对基础模型做任何训练，适合轻量部署。  
- **即插即用**：通过钩子机制实现推理时引导，与提示工程互补，操作方便。  
- **详尽的错误分析**：对手动评估中的失败模式进行了系统分类与统计，为后续改进提供明确方向。  
- **实用性指导**：提供了关于引导强度、提示设计和解码策略的实操经验与推荐范围。

### 8. 不足与局限

- **实验覆盖不足**：未与其他微调范式（如全参数、Adapter）、其他表示工程方法（如线性探测引导）直接对比，难以量化激活引导的相对优势。  
- **单一模型与语言**：仅在 Gemma-3-4B-it 的英文任务上验证，文化特定性未探索。  
- **层选择缺乏系统性**：文中根据试验选择第 16 层，但未提供不同层位的对比实验，难以证明该选择的最优性。  
- **单方向假设**：仅使用 PCA 第一主成分，可能遗漏多维度语义变化信息。  
- **引导强度 α 高度敏感**：需要细致调参，否则易导致语义漂移或退化，不利于全自动部署。  
- **评估指标相关性**：自动评分中 QR 项偏低，反映出对原义保留仍难把控，人工分析也证实了这一点。

（完）
