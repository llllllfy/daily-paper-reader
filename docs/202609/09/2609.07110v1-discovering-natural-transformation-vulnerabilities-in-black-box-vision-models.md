---
title: Discovering Natural Transformation Vulnerabilities in Black-Box Vision Models
title_zh: 发现黑盒视觉模型中的自然变换漏洞
authors: "Dongsu Song, DaeYun GO, Jay Hoon Jung"
date: 2026-09-07
pdf: "https://arxiv.org/pdf/2609.07110v1"
tags: ["query:cp"]
score: 9.0
evidence: ASA搜索颜色和材质变换以发现自然对抗脆弱性
tldr: 自然对抗样本揭示视觉模型在现实语义变化下存在脆弱性，但黑盒生成因自然变换未知及代理依赖而困难。ASA框架采用多模态语言模型与文本引导生成编辑器，通过胜负反馈联合探索背景、天气、材质/颜色变换，并贪心组合仅攻击改进场景。在多个ImageNet分类器上，ASA以更少受害者查询获得更高攻击成功率，且图像和提示均可跨架构迁移。这表明视觉模型具有可重用的自然变换漏洞，ASA能高效黑盒识别并利用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.07110v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1447, \"height\": 896, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.07110v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1447, \"height\": 909, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.07110v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 826, \"height\": 353, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.07110v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1433, \"height\": 344, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.07110v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1429, \"height\": 881, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.07110v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 467, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.07110v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 609, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.07110v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 569, \"height\": 309, \"label\": \"Table\"}]"
motivation: 黑盒生成自然对抗样本因自然变换未知及依赖代理模型或昂贵查询而困难。
method: ASA采用多模态语言模型和文本引导生成编辑器，通过胜负反馈探索背景、天气、材质/颜色变换，并贪心组合攻击改进场景。
result: 在多个ImageNet分类器上，ASA以更少受害者查询实现更高攻击成功率，并展现图像级和提示级可迁移性。
conclusion: 视觉模型存在可重用的自然变换漏洞，ASA能高效黑盒发现并利用这些模式，为黑盒鲁棒性评估提供新思路。
---

## 摘要
自然对抗样本（NAEs）表明，视觉模型可能在超出范数有界扰动的现实语义变化下失效。然而，在黑盒设定下生成自然对抗样本仍具挑战性，因为现有生成式攻击通常依赖代理模型、学习到的攻击先验或代价高昂的基于查询的优化，而暴露模型漏洞的自然变换事先未知。我们提出对抗场景攻击（ASA），这是一种基于查询的黑盒框架，利用多模态语言模型和现代文本引导生成式编辑器在自然语言编辑场景上进行搜索。ASA通过胜者-败者反馈联合探索背景、天气和材质/颜色变换，并使用贪心探索器仅组合能提升攻击效果的场景。在多种ImageNet分类器上，ASA的攻击成功率显著高于先前基于查询的生成式攻击，同时所需受害者模型查询次数更少，并保持有竞争力的感知质量。此外，ASA同时具有图像级和提示级可迁移性：其对抗图像在不同受害者模型架构上仍然有效，而其发现的编辑场景可复用于同类图像，并在某些情况下可跨架构复用。这些发现表明，视觉模型对自然变换模式存在可复用的漏洞，ASA能够在黑盒设定下高效识别这些漏洞。

## Abstract
Natural adversarial examples (NAEs) reveal that vision models can fail under realistic semantic changes beyond norm-bounded perturbations. However, generating NAEs in a black-box setting remains challenging because existing generative attacks often rely on surrogate models, learned attack priors, or costly query-based optimization, whereas the natural transformations that expose model vulnerabilities are unknown a priori. We propose \textbf{Adversarial Scenario Attack (ASA)}, a query-based black-box framework that searches over natural-language editing scenarios using a multimodal language model and a modern text-guided generative editor. ASA jointly explores background, weather, and material/color transformations through winner--loser feedback, and uses a greedy explorer to compose only attack-improving scenarios. Across diverse ImageNet classifiers, ASA achieves substantially higher attack success rates than prior query-based generative attacks while requiring fewer victim-model queries and preserving competitive perceptual quality. Moreover, ASA exhibits both image-level and prompt-level transferability: its adversarial images remain effective across victim-model architectures, while its discovered editing scenarios can be reused across same-class images and, in some cases, across architectures. These findings suggest that vision models possess reusable vulnerabilities to natural transformation patterns, which ASA can efficiently identify in a black-box setting.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：深度视觉模型容易受到对抗攻击，但传统攻击多限制在 \( \ell_p \) 范数球内，忽略了现实语义变化带来的风险。自然对抗样本（Natural Adversarial Examples, NAEs）表明，模型在背景、天气、材质/颜色等自然变换下也可能失效。
- **核心问题**：在黑盒、查询受限的条件下，如何针对给定输入图像生成自然对抗样本。现有方法通常依赖代理模型、攻击先验训练或昂贵的查询优化，且预先不知道哪种自然变换能暴露模型漏洞。
- **整体含义**：论文提出一种基于自然语言编辑场景的搜索框架 ASA，通过多模态语言模型（MLLM）和文本引导生成编辑器直接利用受害者模型的查询反馈来发现自然变换漏洞，为黑盒鲁棒性评估提供了新思路。

## 2. 论文提出的方法论

- **问题定义**：
  - 分数查询黑盒攻击：只能查询分类器 \( F \) 的置信度，不能获取架构、参数或梯度。
  - 攻击目标：寻找自然语言编辑指令 \( p \)，使编辑图像 \( x_p = G(x, p) \) 被错误分类。
  - 攻击分数：采用 margin loss，\( J(p) = \max_{c \neq y} F_c(x_p) - F_y(x_p) \)，\( J(p) > 0 \) 表示攻击成功。
  - 查询预算：每个输入图像最多 100 次查询。

- **ASA 框架（Adversarial Scenario Attack）**：
  - **场景搜索空间**：定义三个编辑策略，\( \mathcal{R} = \{ \text{BG}, \text{WA}, \text{MCR} \} \)，分别对应背景变换、天气与大气、材质与颜色重着色。
  - **历史引导的场景生成器**：每轮迭代，MLLM 根据源图像、真实标签和历史信息 \( H \)，为每个策略生成一个候选自然语言场景 \( p_r^t \)，并通过生成编辑器 \( G \) 生成编辑图像。
  - **胜者-败者反馈**：计算每个候选场景的攻击分数，选分数最高者为胜者 \( p_t^+ \)，其余为败者 \( P_t^- \)。将这些信息及验证反馈写入历史 \( H \)，指导下一轮 MLLM 生成：利用胜者生成更具体变体，并避免败者共享的语义特征。
  - **自然性验证器**：若候选图像使模型误分类，则用 MLLM 验证器检查主对象是否保留、编辑是否仅限于背景/大气/光照/表面外观等自然因素；若不通过，则将反馈加入历史继续搜索。
  - **贪心探索器（Greedy Explorer）**：
    - 候选池 \( \mathcal{C}_t \) 包含当前轮所有策略候选，并从第二轮起加入上一轮贪心场景 \( p_g^{t-1} \)。
    - 选池中得分最高的场景为初始贪心状态 \( g_t^{(0)} \)。
    - 其余候选按个人攻击分数降序遍历，用连接词“and”将当前贪心场景与候选场景组合成复合场景 \( \hat{p} = g \oplus u \)，其中同策略组件替换，不同策略组件追加。
    - 若复合场景攻击分数提高（\( J(\hat{p}) > J(g) \)），则更新贪心状态；否则保持原状。
    - 最终累积的贪心场景作为本轮结果传入下一轮。

- **算法流程**：论文给出了完整伪代码（Algorithm 1），包括候选生成、编辑、评分、验证、贪心组合和预算控制。

## 3. 实验设计

- **数据集**：
  - 查询式与迁移攻击评估：1,000 张 ImageNet 兼容图像，从 299×299 缩放到 224×224。
  - 提示级迁移性：ImageNet 验证集子集 430 类，每类 50 张，共 21,500 张图像。

- **受害者模型**：
  - CNN：ResNet-50、ConvNeXt。
  - Transformer：Swin-Base、DeiT-Base。
  - SSM：Vision Mamba-Small、MambaVision-Base。
  - 鲁棒模型：Adversarial Inception-v3、集成对抗训练 Inception-ResNet-v2。
  - 迁移性实验额外使用：Inception-v3、Wide-ResNet-50、ViT-Base。

- **实现细节**：
  - MLLM：Gemma 4 E4B，未微调，`thinking=false`，`max_new_tokens=4096`，`do_sample=false`。
  - 生成编辑器：FLUX.2 [klein] 9B-KV，输入 1024×1024，种子 123。
  - 搜索设置：每轮每策略生成一个场景，最多 50 轮，查询预算 100 次。

- **对比方法**：
  - 查询式攻击
